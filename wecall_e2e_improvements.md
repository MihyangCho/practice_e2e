# レビュー、疑問点メモ

## 良かったこと
### ①POM（Page object models）構造
- テストスクリプトとUIを分離し読みやすい
- 細部ロジックが隠れてコードが簡潔


## 気になること、その理由
### ①waitForTimeoutの使用
- **ファイル：** form-reservation-check-in.spec.ts
- **理由：** Playwright公式ドキュメントは`waitForTimeout`をデバッグ用途のみ使用し、正式テストでは避けるよう推奨しているため
- **対案：** Playwrightの自動待機(locator auto-waiting / web assertions)に委ねる方向で修正する

### ②useViewportの非同期処理
- **ファイル：** form-reservation-check-in.spec.ts（呼び出し側）、useViewport.ts（定義側)
- **理由：** `page.setViewportSize`は`Promise<void>`を返す非同期関数である。Viewport変更完了前に次のコードが実行 → デスクトップサイズで一時レンダ後にモバイルへ切り替わる。モバイル専用UIセレクタ未発見のリスクあるため
- **対案：** 呼び出し側と定義側両方とも`await`を追加

### ③繰り返しコードによる実行コスト増加
- **ファイル：** *.spec.tsファイルすべて
- **理由：** Context・Pageの生成〜Web受付情報入力またはLINEエントリーのフローがすべてのファイルに登場され、同じコードが実行されている状態のため
- **対策：** フィクスチャ化してコード重複を除去する
<br>⭐️ 実行コストを減少できる箇所

### ④1つのテストファイルでLINE+Web+言語の処理の同時実行による実行コスト増加
- **ファイル：** *.spec.tsファイルすべて
- **理由：** 認証方法（LINE版かWeb版か）や言語の影響を及ぼさないテスト内容まで処理がかかっている気がする
- **対策：** 既存コードでの検証を最低限にする（LINE版は1度のみなど）か、認証方法や言語の検証のためのテストコードを別途実装する
<br>⭐️ 実行コストを減少できる箇所

### ⑤テスト内容がテスト名との一致性が低い
- **ファイル：** form-reservation-check-in.spec.ts
- **理由：** 【フォーム入力 → 予約 → 予約内容変更 → 予約キャンセル → 再予約 → チェックイン】というステップを検証するが、テスト名（「フォーム + 予約 + チェックインフロー」）とマッチしていないため
<br>-----
- **ファイル：** invalid-form-reservation-check-in.spec.ts
- **理由：** 無効値入力時の挙動（エラー）を確認したいファイルに見えるが、テスト内容としては正しい値を再入力しチェックインまで確認しているため
<br>-----
- **対策：** テスト内容とテスト名を一致させる（テスト範囲を確かめにする）

### ⑥入力バリデーションの不十分さ
- **ファイル：** invalid-form-reservation-check-in.spec.ts
- **理由：** フォーム入力欄に対してすべてのバリデーションを網羅していない状態のため
- **対策：** 境界値分析などを使ってすべての入力欄バリデーションが含まれるinvalidUserInfoListを作成（膨大にならないように）

### ⑦処理ステップが多くて実行コストがかかりそう
- **ファイル：** form-reservation-check-in.spec.ts
- **理由：** 【フォーム入力 → 予約 → 予約内容変更 → 予約キャンセル → 再予約 → チェックイン】というステップを検証するが、前でNGになったらその後は未実行されるため
- **対策：** テスト内容に合わせて処理を分割したほうがよさそう（予約キャンセルまで、予約変更までなど）（→テストケース膨大になっちゃうか？）
<br>⭐️ 実行コストを減少できる箇所
